{
  "name": "olx-flipper-club",
  "private": true,
  "version": "0.1.0",
  "description": "Public flip-deal feed of OLX/SV candidates — one car at a time, Stripe deposit per listing to unlock the seller contact.",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create KV",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
